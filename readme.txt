Explication de la classe Ingredient:
J'ai créé une classe Java que j'ai appelée Ingredient. Cette classe est destinée à représenter les différents ingrédients que l'on pourrait utiliser dans un projet de pizza.

Déclaration des attributs de la classe :
J'ai ajouté quatre attributs à ma classe Ingredient : codeingr, nomingr, uniteingr, et qtecomp. Chacun de ces attributs stocke des informations spécifiques sur un ingrédient.

Constructeur de la classe Ingredient :
J'ai créé un constructeur qui prend en paramètre les valeurs initiales des attributs de l'objet Ingredient et les initialise lors de la création de l'objet.

Méthodes d'accès (getters et setters) :
J'ai ajouté des méthodes d'accès (getters et setters) pour chaque attribut, permettant ainsi d'obtenir et de modifier les valeurs de ces attributs depuis l'extérieur de la classe.

Méthode toString :
J'ai surchargé la méthode toString pour fournir une représentation textuelle de l'objet Ingredient. Cette méthode renvoie une chaîne de caractères formatée avec les informations importantes sur l'ingrédient.

Fin de la classe Ingredient :
J'ai terminé la déclaration de ma classe Ingredient.

En résumé, j'ai créé une classe Java appelée Ingredient pour représenter les ingrédients d'une pizza, avec des attributs pour stocker les informations, un constructeur pour initialiser ces informations, des méthodes d'accès pour obtenir et modifier ces informations, et une méthode toString pour obtenir une représentation textuelle de l'objet.








EXPLICATION CLASSE PIZZA

J'ai créé une classe Java nommée Pizza pour représenter une pizza. Elle possède des attributs tels que le numéro (nroPizza), la conception (designPizza), le tarif (tarifPizza), et une liste d'ingrédients (maListeingredient).

J'ai ajouté un constructeur pour initialiser ces attributs lors de la création d'un objet Pizza. Des méthodes d'accès (getters et setters) sont fournies pour obtenir et modifier les valeurs des attributs.

La méthode toString a été surchargée pour obtenir une représentation textuelle de l'objet Pizza. En résumé, cette classe simplifie la gestion des informations liées à une pizza, facilitant ainsi la création et la manipulation d'objets Pizza dans un programme.





EXPLICATION CONTROLEUR.JAVA
Le code représente un programme principal en Java. Il crée une liste de pizzas en utilisant une méthode de la classe Controllers et affiche ensuite cette liste avec des détails tels que les prix et les ingrédients de chaque pizza.













